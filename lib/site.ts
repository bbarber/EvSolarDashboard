/**
 * Single source of truth for this project's identity.
 *
 * `pnpm run init` rewrites these values when a new project is created from the
 * template; after that, edit them here by hand. They feed the document title,
 * the nav brand, and metadata — change them in one place, not scattered around.
 */
export const SITE_NAME = 'EvSolar Dashboard';
export const SITE_DESCRIPTION =
  'Solar-matched Tesla charging: live status, production, and decisions.';

/**
 * Where users land after a successful login or password update. Change this to
 * your app's authenticated home route.
 */
export const AUTH_REDIRECT_PATH = '/';
